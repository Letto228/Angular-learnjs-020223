import { ItemsFilterByPropertyPipe } from './items-filter-by-property.pipe';

describe('ItemsFilterByPropertyPipe', () => {
	it('create an instance', () => {
		const pipe = new ItemsFilterByPropertyPipe();
		expect(pipe).toBeTruthy();
	});
});
