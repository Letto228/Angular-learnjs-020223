import { EndlessScrollDirective } from './endless-scroll.directive';

describe('EndlessScrollDirective', () => {
	it('should create an instance', () => {
		const directive = new EndlessScrollDirective();
		expect(directive).toBeTruthy();
	});
});
