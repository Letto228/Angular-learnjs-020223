import { ScrollLoadingDirective } from './scroll-loading.directive';

describe('InsertScrollLoading', () => {
	it('should create an instance', () => {
		const directive = new ScrollLoadingDirective();
		expect(directive).toBeTruthy();
	});
});
