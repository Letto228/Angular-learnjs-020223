import { ItemsFilterPipe } from './items-filter.pipe';

describe('ItemsFilterPipe', () => {
	it('create an instance', () => {
		const pipe = new ItemsFilterPipe();
		expect(pipe).toBeTruthy();
	});
});
